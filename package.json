{
  "name": "turbohop",
  "private": true,
  "version": "0.7.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:firebase": "tsc && vite build",
    "build:github": "BASE_PATH=/TurboHop/ tsc && BASE_PATH=/TurboHop/ vite build",
    "test": "vitest run",
    "preview": "vite preview",
    "deploy:firebase": "firebase deploy --only hosting"
  },
  "dependencies": {
    "@fontsource/press-start-2p": "^5.2.7",
    "firebase": "^12.11.0",
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "jsdom": "^29.0.1",
    "playwright": "1.56.1",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vitest": "^4.1.2"
  }
}
